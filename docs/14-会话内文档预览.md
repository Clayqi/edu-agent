# 会话内文档预览（v1.13.0 · 2026-09-12）

**AI 生成文档后，会话页右侧自动出现一个可拖拽的多格式预览面板。**
看稿在会话里完成；要交出去的 .docx / .pptx 再点面板上的导出按钮落盘。

> 与 `docs/08-WPS集成说明.md` 的分工：08 讲 **WPS 落盘导出**（能力开关、COM、桥接补丁、坑）；
> 本文讲**会话内内存预览**（面板、四页签、开关）。两条链路严格分开，见 §1。

---

## 1. 两条链路（本改造的核心约束）

| | **会话内内存预览** | **WPS 落盘导出** |
|---|---|---|
| 目的 | 生成完就能看 | 拿到能交出去的文件 |
| 触发 | AI 出文档后自动唤起；也可点标题栏按钮切换页签 | 点面板上的 `Word` / `PPT` |
| 接口 | `POST /api/session/doc-preview` | `POST /api/wps/export` + `GET /api/wps/download` |
| 磁盘 | **不写任何文件** | 写 `content/plans/` 或自选目录 |
| WPS | **不调用、不查 WPS 开关** | 依赖 `wps-office` MCP（关掉即拒） |
| 能力开关 | `skill: doc-session-preview`（只管面板渲不渲染） | `mcp: wps-office` |

**双向解耦**（`tests/test_wps_tools.py::TestSessionDocPreview` 有用例锁住）：

- 关掉 **WPS** → 预览照常可用；点导出在**发请求前**就被拦下，并明说原因；
- 关掉 **会话预览** → 面板不渲染；`/api/wps/export` 行为一字不变。

---

## 2. 界面

```
会话页（#view-chat）
├─ 消息区（面板打开时右侧让位，--docp-w 控制留白）
└─ #docPanel  ← 右侧固定面板，可拖拽左边缘调宽（双击复位，宽度记忆在 localStorage: edu_docpanel_w）
   ├─ 标题栏：标题 / 字数·页数 + [导入] [下载▾] [位置] [×]
   │   └─ 鼠标悬停「下载」→ 弹出下载方式菜单（点击也能开，兼容触屏）
   ├─ 页签：文档 | Markdown | 幻灯片 | 结构
   ├─ #docPanelBody：内容区
   └─ #docPanelRes：下载结果条（文件名 + 目录 + 「下载到本机」）
```

面板支持**两种文档来源**，页签内容随之适配：

| 来源 | 进入方式 | 文档 | Markdown | 幻灯片 | 结构 | 下载走哪条 |
|---|---|---|---|---|---|---|
| **会话教案**（Markdown） | AI 出教案后自动唤起 | ✅ 排版渲染 | ✅ 原文 | ✅ 每环节一页 | ✅ | `markdown:` → WPS |
| **导入的 .docx** | 标题栏「导入」 | ✅ 逐 run 保格式（含真表格） | —（无 md 源） | —（按板块标题成页） | ✅ | `template:` → 原格式直出 |

| 页签 | 内容 |
|---|---|
| **文档** | 会话教案用 `mdLight()` 渲染；导入的 .docx 用 `docpRichHtml()` 逐 run 渲染（加粗/斜体/下划线/字体/字号/颜色/对齐/表格） |
| **Markdown** | 会话教案的原文（可复制）；导入的文档会说明「没有 Markdown 源，看文档页签」 |
| **幻灯片** | 逐页卡片（每环节一页）+「在新窗口看（16:9）」，来自服务端 `slides_from_markdown()` |
| **结构** | 板块清单 + 每块字数 + 总计 |

标题栏按钮（**下载出口已合并成一个**）：

| 按钮 | 行为 |
|---|---|
| **导入** | 选一个 `.docx` → 服务端解析成富结构 → **只在面板里预览/下载**（不建模板、不进模板库） |
| **下载 ▾** | **鼠标悬停弹出下载方式菜单**，三选一：<br>· **Word（.docx）** 经 WPS 导出 · 落盘<br>· **PPT（.pptx）** 本地生成 · 每环节一页<br>· **HTML** 服务端网页版 · 直接下载（不依赖 WPS，仅会话教案有）<br>落盘的两项在 `wps-office` 未就绪时**灰显**，但**仍可点**——点了会在结果条里说明原因 |
| **位置** | 选择导出目录（记忆 `edu_dz_outdir`） |
| **×** | 关闭面板 |

**拖拽调宽**：按住面板左边缘拖动（`#docPanelGrip`），宽度夹在 320 ~ min(920, 74vw)，双击复位 460。

---

## 3. 接口

### `POST /api/session/doc-preview`

纯内存：**不落盘、不调用 WPS MCP、不查 WPS 能力开关**；输入原样回传 + 派生预览模型。

```jsonc
// 请求
{"payload": {...}, "markdown": "文本", "session_id": "…"}   // markdown 留空时会话里最近一份教案兜底
// 响应
{"ok": true,
 "enabled": true,                 // = skill 开关 doc-session-preview，前端据此决定渲不渲染
 "title": "3.3 幂函数",
 "markdown": "…",                 // 原样回传
 "payload": {...},                // 原样回传
 "blocks": [{"title": "教学目标", "level": 2, "chars": 43}],
 "slides": [{"name": "情境导入", "minutes": "5", "bullets": ["…"]}],
 "stats": {"chars": 222, "blocks": 9, "slides": 3, "minutes": 20},
 "note": "内存预览：不落盘、不调用 WPS；需要落盘请走 /api/wps/export"}
```

> 刻意**不在接口层加闸门**：接口是纯函数，关掉开关只影响前端渲不渲染（响应里的 `enabled=false`）。
> 这样「关掉预览」不会连带影响任何导出调用。

### `GET /api/mcp/status`（改动）

新增字段 `session_doc_preview_ready`（= `skill: doc-session-preview` 开关状态）；
`GET /api/capabilities` 的快照里同样带这个字段。

---

## 4. 能力开关

新增 **skill**：`doc-session-preview`（名称「会话文档预览」），**默认开启**，在 **Skill 市场**里可开关。

- 它是**内置能力**（没有 `skills/<id>/SKILL.md` 实体），`snapshot()` 里带 `builtin: true`，
  UI 显示「内置」而不是「文件缺失」；
- 不绑定任何 MCP 服务，所以不会因为关掉 WPS 而显示「依赖已关」；
- 关掉后：会话页**不渲染**右侧面板，导出链路**完全不受影响**。

---

## 5. 教案中心：页面保留，只移除了「模板编辑器」

「教案中心」页面（导航项 `design`）与 **教案 × PPT 编排台** **完整保留**，不再改动；
本次只从里面移除了**模板编辑器**这一块：

| 教案中心页面里的东西 | 处置 |
|---|---|
| 教案 × PPT 编排台（课题/学情、生成骨架、＋添加环节、保存、导出 HTML、预览 PPT、导出 Word·WPS、生成 PPT·WPS、导出位置；两栏编辑器） | **保留**（原样） |
| **导入 .docx** | **保留**（模板行里现在只有它） |
| 模板下拉 / 载入编辑 / 新建空白 / 删除 / 保存为模板 / 退出模板模式 / 格式工具栏 / 模板编辑器 | **移除** |

**「导入 .docx」的行为**：解析成富结构后**只进右侧预览面板**（看 + 下载，下载走 `template:` 按原格式直出），
**不写入模板库**。预览面板在**全局层**（不在会话页内部），所以在教案中心导入也能就地看到。

**边界**（照实说）：

- 后端 `/api/template/*` 一行未动，`content/templates/` 里的模板文件也**没删**；
- 前端**不再有**「把 .docx 导入成模板 / 编辑板块·段落·格式 / 另存为模板 / 删除模板」的入口；
- 「① 生成教案模板」（后端生成并落库）与「② 按模板生成」（Agent B 套用所选模板）**后端仍然可用**，
  但**会话里已经没有这张「下一步」卡片了**（2026-09-19 去掉，见 `docs/16-…计划书.md` §5.5），
  所以这三个按钮在界面上没有入口，教案模板相关操作从**教案中心**走；
- 「③ 填入」的实现仍在（结果进右侧预览面板），同样随卡片一起不再出现在会话里。

另外：对话里原先挂在回答下方的「导出条」已移除，改由预览面板承担（一是避免两处入口，
二是它现在有贴面板边缘的把手入口，不再"关掉就找不回来"）。

**右下角常驻入口**（会话页与教案中心都可见）：

| 状态 | 文案 | 点击行为 |
|---|---|---|
| 还没有文档 | **导入文档** | 直接开文件选择（解决"想导入却没有入口"） |
| 有文档、面板收起 | **查看文档** | 打开右侧预览面板 |
| 有文档、面板打开 | **收起文档** | 收起面板（内容还在，随时再看） |

---

## 6. 测试

| 文件 | 例数 | 覆盖 |
|---|---|---|
| `tests/test_wps_tools.py` | 32 | 原 25 例 + 新增 `TestSessionDocPreview` 7 例：能力在册且为内置 / 状态字段 / **纯函数（不落盘、不调 MCP）** / 原样回传 / **WPS 关掉预览照常** / **预览关掉导出照常** / 状态接口字段 |

jsdom 真 DOM 回归（临时脚本，不入库）5 个场景，全过：

1. 教案中心页面已下线（导航项与 `#view-design` 都不在）；面板初始隐藏，B 回答后**自动唤起**，标题/统计正确；
2. 四个页签内容正确（幻灯片 3 页含环节名）、拖拽 460→320 并记忆、`--docp-w` 生效、Word/PPT 请求体正确；
3. 模板抽屉：初始隐藏、通过「模板」按钮打开、关闭正常、下拉已载入模板；
4. 关掉「会话文档预览」→ 面板不渲染；
5. 关掉 WPS → 预览仍可用，导出**前置拦下（0 个请求）**且明说原因。

全量：**215 例通过**（skipped=2 为需真 WPS 的用例）。

---

## 7. 排错

| 现象 | 原因 / 处理 |
|---|---|
| AI 出了文档但右侧没面板 | ① 设置里「会话文档预览」被关了（Skill 市场可开）；② 该回答不是文档类（只有 Agent B 出的教案会自动唤起）。关掉了也能用右下角入口再看 |
| 面板挡住了消息 | 拖左边缘调窄，或直接关掉；宽度会记住，双击左边缘复位 460 |
| 「下载」按钮上悬停没反应 | 菜单是 CSS `:hover` 弹出的，鼠标要停在按钮上；触屏/点击也能开（再点一次收起） |
| 下载菜单里 Word/PPT 是灰的 | `wps-office` 服务未就绪 → 去「MCP 服务」开启；**HTML 与预览不受影响**，点灰项也会说明原因 |
| 点 Word/PPT 报「WPS Office 服务已关闭」 | 去「MCP 服务」开启 `wps-office`；**预览不受影响**，可以继续看 |
| 「HTML」说没有服务端 HTML | 该回答不是教案 Agent 出的（服务端只在 B 出教案时落 HTML 页）。用 Word/PPT 落盘即可 |
| 找不到模板编辑器了 | 已按维护者要求移除（只要模板编辑能力不要了），**教案中心页面与编排台都还在**；导入 .docx 用教案中心的「导入 .docx」或右下角入口 |
| 想导入自己的 .docx 来看/下载 | 教案中心「导入 .docx」或右下角「导入文档」（只进预览，不进模板库） |
